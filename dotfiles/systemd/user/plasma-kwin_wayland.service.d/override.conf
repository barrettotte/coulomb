[Service]
# Pin the 3090 Ti (PCI 0B:00.0) as KWin's primary render device. Otherwise
# KWin picks the lower-PCI card and 3090 Ti scanout breaks under
# cross-vendor PRIME. by-path/ symlinks are stable across DRM renumbering.
Environment=KWIN_DRM_DEVICES=/dev/dri/card2:/dev/dri/card1
